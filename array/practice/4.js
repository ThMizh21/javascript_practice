// Create an array of 4 car brands. Remove the last element using pop() and print the removed element as well as
//  the updated array.

let cars=["BMW","Audi","Jaguar","Tesla"];
let lastcar=cars.pop();
console.log(lastcar);
console.log(cars);
